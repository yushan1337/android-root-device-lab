[project]
name = "android-root-device-lab"
version = "0.1.0"
description = "ADB-based Android device diagnostics toolkit"
requires-python = ">=3.11"

[project.scripts]
android-device-lab = "android_device_lab.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
